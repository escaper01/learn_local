# Chapter 12 assessment

Before touching the debugger, use this chapter's rules on whole-region regex matching to predict what **Whole-region grammar** should print, then run it and repair the defect so the check validates the entire input rather than a matching substring.

The courseCode function lab exercises one whole-input pattern in isolation; it is not proof of every competency from path traversal defense through explicit serialization schemas covered across the chapter's five lessons. Review the repaired source and explain why matches() versus find() changes the outcome, without consulting the answer.
