# Chapter 24 assessment

Bring together bean wiring, auto-configuration, DTO validation, transaction proxy boundaries, and JPA entity/version semantics to solve the decision scenario and repair **Optimistic update must use the expected version**, where an update statement omits the version check needed to detect a conflicting write. Write a prediction before running the debugger and preserve the intended optimistic-locking behavior when fixing it.

The function exercise is a bounded practice task, not proof of every chapter competency. Complete the implementation and external practice described in the five lessons as well. Review the changed source for its contract, then explain the relevant tradeoff without consulting the answer.
