{
  "id": "module-24-spring-boot-and-jpa-foundations",
  "title": "Chapter 24: Spring Boot and JPA Foundations",
  "description": "Build a Spring Boot vertical slice without surrendering core Java and SQL reasoning. Learners wire required beans through constructors, explain conditional auto-configuration and property precedence, translate validated HTTP DTOs, define service and transaction boundaries, choose focused test slices, and predict JPA entity state, generated SQL, fetching behavior, optimistic locking, and migration compatibility.",
  "lessons": [
    {
      "id": "m24-l01-spring-container-beans-scopes-lifecycle-proxies-and-construc",
      "title": "Spring container, beans, scopes, lifecycle, proxies, and constructor injection",
      "theoryMarkdown": "# Spring container, beans, scopes, lifecycle, proxies, and constructor injection\n\n## Dependency injection is object construction\nSpring's container discovers or receives bean definitions, creates instances, resolves dependencies, and manages configured lifecycle callbacks. Constructor injection makes required collaborators visible and permits ordinary unit tests.\n```java\n@org.springframework.stereotype.Service\nfinal class CompleteTask {\n    private final TaskRepository tasks;\n    CompleteTask(TaskRepository tasks) { this.tasks = tasks; }\n}\n```\nThis fragment requires Spring and an application-defined repository. In current supported Spring versions, a single constructor can be used without an explicit @Autowired. Pin and verify the framework version in the external project.\n\n## Scope and proxies\nThe default singleton scope is one instance per container, not one instance for all JVMs. Such services may serve concurrent requests and should not store mutable per-request state. Proxies wrap beans to provide behavior such as transactions; calls must cross the relevant proxy boundary.\n\n## Practice\nConstruct CompleteTask manually with a fake repository, then wire it through a small application context. Introduce two repository implementations and resolve the ambiguity explicitly. Explain bean destruction ownership and why field injection makes required dependencies less visible.",
      "exercises": [
        {
          "id": "m24-l1-check",
          "type": "multipleChoice",
          "title": "Spring container, beans, scopes, lifecycle, proxies, and constructor injection: concept check",
          "instructionMarkdown": "What does Spring's default singleton scope mean?",
          "choices": [
            "A fresh instance for every method call",
            "One bean instance per container",
            "One instance across all processes"
          ],
          "correctChoice": 1,
          "hints": [
            "Scope this to a single application context: singleton describes how many instances that one container creates, not how many JVMs share it."
          ]
        }
      ]
    },
    {
      "id": "m24-l02-boot-auto-configuration-properties-profiles-validation-and-s",
      "title": "Boot auto-configuration, properties, profiles, validation, and startup",
      "theoryMarkdown": "# Boot auto-configuration, properties, profiles, validation, and startup\n\n## Auto-configuration is conditional wiring\nSpring Boot chooses configuration based on the classpath, properties, existing beans, and other conditions. It does not remove the need to understand what was created. Inspect startup diagnostics and configuration reports when behavior differs from expectations.\n```java\n@org.springframework.boot.context.properties.ConfigurationProperties(\"client\")\nrecord ClientSettings(java.net.URI baseUri, java.time.Duration timeout) {\n    ClientSettings {\n        if (timeout == null || timeout.isZero() || timeout.isNegative())\n            throw new IllegalArgumentException(\"positive timeout required\");\n    }\n}\n```\nRegister configuration properties through the supported scanning or enabling mechanism. This is an external Spring Boot fragment, not a standard-library exercise.\n\n## Configuration precedence\nFiles, environment, command-line input, and test overrides have defined precedence in the selected Boot version. Profiles group configurations but should not conceal incompatible schemas or secrets. Bind related settings into typed values and validate at startup.\n\n## Practice\nOverride a timeout through a test configuration and verify the effective value. Make the value invalid and require startup to fail with a useful message. Record the chosen Boot version and Java 21 compatibility. Keep credentials outside packaged defaults and redact them from configuration diagnostics.",
      "exercises": [
        {
          "id": "m24-l2-check",
          "type": "multipleChoice",
          "title": "Boot auto-configuration, properties, profiles, validation, and startup: concept check",
          "instructionMarkdown": "Why validate bound configuration at startup?",
          "choices": [
            "Bad required values should fail before serving requests",
            "It eliminates all runtime errors",
            "It makes profiles identical"
          ],
          "correctChoice": 0,
          "hints": [
            "Compare a service that fails fast during startup on a zero or negative timeout against one that only discovers the bad value the first time it is used mid-request."
          ]
        }
      ]
    },
    {
      "id": "m24-l03-rest-controllers-dtos-bean-validation-and-exception-response",
      "title": "REST controllers, DTOs, Bean Validation, and exception responses",
      "theoryMarkdown": "# REST controllers, DTOs, Bean Validation, and exception responses\n\n## Controllers translate transport contracts\n```java\nrecord CreateTaskRequest(\n    @jakarta.validation.constraints.NotBlank String title) {}\n@org.springframework.web.bind.annotation.RestController\nfinal class TaskController {\n    private final CreateTask useCase;\n    TaskController(CreateTask useCase) { this.useCase = useCase; }\n    @org.springframework.web.bind.annotation.PostMapping(\"/tasks\")\n    @org.springframework.web.bind.annotation.ResponseStatus(\n        org.springframework.http.HttpStatus.CREATED)\n    TaskResponse create(\n        @jakarta.validation.Valid\n        @org.springframework.web.bind.annotation.RequestBody CreateTaskRequest request) {\n        return useCase.handle(request.title());\n    }\n}\n```\nThe application-specific use case and response plus Spring/validation dependencies must exist. The controller accepts a DTO, validates shape, delegates business behavior, and returns an explicit transport result. It should not serialize a persistence entity accidentally.\n\n## Errors and authorization\nCentral exception handling can map domain errors to stable status codes and error bodies. Bean Validation does not authorize a caller or prove uniqueness in a database. Keep those responsibilities in their appropriate boundaries.\n\n## Practice\nTest valid creation, blank title, malformed JSON, unauthorized access, and duplicate/conflicting requests. Verify 201 and the response schema for success, safe error messages for failure, and no stack trace or internal entity fields in responses.",
      "exercises": [
        {
          "id": "m24-l3-check",
          "type": "multipleChoice",
          "title": "REST controllers, DTOs, Bean Validation, and exception responses: concept check",
          "instructionMarkdown": "Does Bean Validation authorize the caller?",
          "choices": [
            "Yes; @Valid implies ownership",
            "Yes; it logs in the user",
            "No; it checks declared validation constraints"
          ],
          "correctChoice": 2,
          "hints": [
            "Separate what @Valid checks about the shape of a request body from who the caller is and what that caller is permitted to do."
          ]
        }
      ]
    },
    {
      "id": "m24-l04-services-repositories-transaction-proxy-boundaries-and-testi",
      "title": "Services, repositories, transaction proxy boundaries, and testing slices",
      "theoryMarkdown": "# Services, repositories, transaction proxy boundaries, and testing slices\n\n## A service represents a use case\nA service coordinates domain operations and persistence. A repository offers needed storage operations. The transaction should cover the invariant and be kept short.\n```java\n@org.springframework.transaction.annotation.Transactional\npublic void complete(long id) {\n    Task task = repository.find(id).orElseThrow();\n    task.complete();\n    repository.save(task);\n}\n```\nThis member fragment belongs to a configured Spring bean. In default proxy mode, an internal this.complete(...) call bypasses transaction interception. Defaults commonly roll back on unchecked exceptions and Error, not every checked exception; configure and test the intended policy for the pinned version.\n\n## Test layers\nPlain unit tests exercise decisions without starting Spring. Web slices test request mapping and validation; persistence slices test mapping; full-context tests verify essential wiring. A mock repository cannot establish transactional behavior, and an in-memory database can differ from the production engine.\n\n## Practice\nInject failure after one update and verify rollback against the chosen database. Call the method through a bean and through self-invocation and explain the distinction. Document transaction propagation and avoid a slow external HTTP call while holding database locks.",
      "exercises": [
        {
          "id": "m24-l4-check",
          "type": "multipleChoice",
          "title": "Services, repositories, transaction proxy boundaries, and testing slices: concept check",
          "instructionMarkdown": "Does this.complete() necessarily trigger default Spring transaction interception?",
          "choices": [
            "Only if the method returns void",
            "No; self-invocation bypasses the proxy",
            "Yes; the annotation changes every JVM call"
          ],
          "correctChoice": 1,
          "hints": [
            "Trace the call path: a self-invocation through this never passes through the surrounding proxy that default Spring AOP relies on to add transaction behavior."
          ]
        }
      ]
    },
    {
      "id": "m24-l05-jpa-entity-states-mappings-fetching-n-1-locking-and-migratio",
      "title": "JPA entity states, mappings, fetching, N+1, locking, and migrations",
      "theoryMarkdown": "# JPA entity states, mappings, fetching, N+1, locking, and migrations\n\n## ORM still executes SQL\nAn entity can be transient, managed, detached, or removed. A persistence context tracks managed state and identity; changes may flush later than the setter call.\n```java\n@jakarta.persistence.Entity\nclass TaskEntity {\n    @jakarta.persistence.Id @jakarta.persistence.GeneratedValue\n    private Long id;\n    @jakarta.persistence.Version\n    private long version;\n    @jakarta.persistence.Column(nullable = false, length = 200)\n    private String title;\n    protected TaskEntity() {}\n}\n```\nThis Jakarta Persistence fragment needs a provider, database, and complete application configuration. Versioning can detect conflicting writes. Entity equality and generated IDs require care; records and DTOs often serve different roles.\n\n## Fetch deliberately\nLazy relations can produce N+1 queries or fail outside the persistence context. Eager relations can overfetch. Choose fetch joins, projections, or entity graphs per use case and measure generated SQL. Cascading operations differ from foreign-key cascades.\n\n## Practice\nCreate one owner with many tasks and record query counts for a report. Fix an N+1 case without globally marking every relationship eager. Test optimistic conflict and schema migration on the real database engine. Version changes with migrations instead of relying on automatic schema mutation in production.",
      "exercises": [
        {
          "id": "m24-l5-check",
          "type": "multipleChoice",
          "title": "JPA entity states, mappings, fetching, N+1, locking, and migrations: concept check",
          "instructionMarkdown": "What does an entity @Version field support?",
          "choices": [
            "Optimistic conflict detection",
            "Automatic elimination of all queries",
            "HTTP API version selection"
          ],
          "correctChoice": 0,
          "hints": [
            "Think about how comparing the version value read at load time against the row's current version at update time reveals a concurrent conflicting write."
          ]
        }
      ]
    },
    {
      "id": "m24-assessment",
      "title": "Chapter 24 assessment and deliberate practice",
      "theoryMarkdown": "# Chapter 24 assessment\n\nBring together bean wiring, auto-configuration, DTO validation, transaction proxy boundaries, and JPA entity/version semantics to solve the decision scenario and repair **Optimistic update must use the expected version**, where an update statement omits the version check needed to detect a conflicting write. Write a prediction before running the debugger and preserve the intended optimistic-locking behavior when fixing it.\n\nThe function exercise is a bounded practice task, not proof of every chapter competency. Complete the implementation and external practice described in the five lessons as well. Review the changed source for its contract, then explain the relevant tradeoff without consulting the answer.",
      "exercises": [
        {
          "id": "m24-judgment",
          "type": "multipleChoice",
          "title": "Spring Boot and JPA Foundations: professional judgment",
          "instructionMarkdown": "A transactional method is called through this. What should be tested?",
          "choices": [
            "Whether the call crosses the configured proxy and transaction boundary",
            "Only whether the annotation text exists",
            "Only whether the method is short"
          ],
          "correctChoice": 0
        },
        {
          "id": "m24-coding-lab",
          "type": "function",
          "title": "Chapter 24 implementation lab: httpStatus",
          "instructionMarkdown": "Implement `httpStatus`. Map CREATED to 201 and MISSING to 404; every other result maps to 500. Write the precondition and boundary table first, keep the method deterministic, and do not print.\n\nThis pure mapping helper does not test Spring controllers. Complete the external web validation and transaction labs before considering the chapter complete.",
          "starterFiles": [
            {
              "path": "Solution.java",
              "content": "public final class Solution {\n    public static int httpStatus(String value1) {\n        return 0;\n    }\n}\n"
            }
          ],
          "entrypoint": {
            "kind": "function",
            "className": "Solution",
            "name": "httpStatus",
            "parameters": [
              {
                "name": "value1",
                "type": "string"
              }
            ],
            "returns": "int"
          },
          "tests": [
            {
              "id": "m24-code-1",
              "visibility": "public",
              "arguments": [
                "CREATED"
              ],
              "expected": 201
            },
            {
              "id": "m24-code-2",
              "visibility": "public",
              "arguments": [
                "MISSING"
              ],
              "expected": 404
            },
            {
              "id": "m24-code-3",
              "visibility": "hidden",
              "arguments": [
                "x"
              ],
              "expected": 500
            },
            {
              "id": "m24-code-4",
              "visibility": "hidden",
              "arguments": [
                "CONFLICT"
              ],
              "expected": 500
            },
            {
              "id": "m24-code-5",
              "visibility": "hidden",
              "arguments": [
                "created"
              ],
              "expected": 500
            }
          ],
          "hints": [
            "Map each named result string to its status code individually, and let any string that is not exactly \"CREATED\" or \"MISSING\" fall through to the 500 default.",
            "Check that comparison is case-sensitive, since a lowercase \"created\" is a distinct string from \"CREATED\" and must also map to 500."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        },
        {
          "id": "m24-debug-lab",
          "type": "debug",
          "title": "Optimistic update must use the expected version",
          "instructionMarkdown": "The supplied SQL increments the version column but never checks it in the WHERE clause, so a concurrent conflicting write would be silently overwritten instead of detected. Repair the statement so the program writes exactly `UPDATE task SET title = ?, version = version + 1 WHERE id = ? AND version = ?`. Preserve the demonstrated optimistic-locking predicate and explain why your fix changes the result; do not replace it with a hard-coded print. Output tests verify the result; preserving the intended optimistic-locking behavior requires source review.",
          "starterFiles": [
            {
              "path": "Main.java",
              "content": "public class Main {\n    \n    public static void main(String[] args) throws Exception {\n        String sql=\"UPDATE task SET title = ?, version = version + 1 WHERE id = ?\";System.out.print(sql);\n    }\n}\n"
            }
          ],
          "tests": [
            {
              "id": "m24-debug-test",
              "visibility": "public",
              "input": "",
              "expected": "UPDATE task SET title = ?, version = version + 1 WHERE id = ? AND version = ?",
              "comparison": "exact"
            }
          ],
          "hints": [
            "The expected version belongs in the update predicate; later binding and row-count checks are also required."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        }
      ]
    }
  ]
}
