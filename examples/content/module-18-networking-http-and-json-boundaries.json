{
  "id": "module-18-networking-http-and-json-boundaries",
  "title": "Chapter 18: Networking, HTTP, and JSON Boundaries",
  "description": "Specify process boundaries that remain correct under partial reads, malformed data, slow peers, and dependency failure. Learners frame TCP messages, apply HTTP method and status semantics, use Java HttpClient with deadlines, validate bounded JSON DTOs, and combine idempotency, retry classification, exponential backoff, jitter, circuit breaking, and concurrency isolation responsibly.",
  "lessons": [
    {
      "id": "m18-l01-tcp-sockets-byte-stream-framing-timeouts-limits-and-cleanup",
      "title": "TCP sockets, byte-stream framing, timeouts, limits, and cleanup",
      "theoryMarkdown": "# TCP sockets, byte-stream framing, timeouts, limits, and cleanup\n\n## TCP has no message boundaries\nA single write can be split across reads, and several writes can arrive in one read. Your protocol must define length-prefix or delimiter framing, encoding, size limits, EOF behavior, and timeouts.\n```java\ntry (var socket = new java.net.Socket()) {\n    socket.connect(new java.net.InetSocketAddress(\"localhost\", 9000), 2000);\n    socket.setSoTimeout(3000);\n    var input = new java.io.DataInputStream(socket.getInputStream());\n    int length = input.readInt();\n    if (length < 0 || length > 65536) throw new java.io.IOException(\"bad frame\");\n    byte[] body = new byte[length];\n    input.readFully(body);\n}\n```\nThis external lab needs a cooperating local server. readFully either fills the frame or fails on premature EOF. Validate the length before allocation. Closing the socket closes its streams.\n\n## Practice\nWrite a local server that deliberately sends a frame in small chunks. Test zero length, maximum length, truncated data, and a silent peer. Explain why available() is not the total message length and why a read timeout does not necessarily bound the whole request's elapsed lifetime.",
      "exercises": [
        {
          "id": "m18-l1-check",
          "type": "multipleChoice",
          "title": "TCP sockets, byte-stream framing, timeouts, limits, and cleanup: concept check",
          "instructionMarkdown": "Can one TCP read be assumed to contain one complete application message?",
          "choices": [
            "Only if UTF-8 is used",
            "No; framing and partial-read handling are required",
            "Yes; every write is preserved as a read"
          ],
          "correctChoice": 1,
          "hints": [
            "Consider what happens when a peer's single write is larger than a network buffer, and why one socket read is not guaranteed to match one application write."
          ]
        }
      ]
    },
    {
      "id": "m18-l02-http-methods-status-codes-headers-media-types-and-idempotenc",
      "title": "HTTP methods, status codes, headers, media types, and idempotency",
      "theoryMarkdown": "# HTTP methods, status codes, headers, media types, and idempotency\n\n## HTTP carries semantics\nGET retrieves and should be safe; PUT replaces a resource and is idempotent by contract; POST commonly creates or executes an operation and is not assumed idempotent. Idempotent means repeating the request has the same intended effect, not that every response is identical.\n\n```http\nPOST /tasks HTTP/1.1\nContent-Type: application/json\nAccept: application/json\n\n{\"title\":\"Review\"}\n```\nA creation response may use 201 and a Location header. Distinguish 400 malformed input, 401 authentication required, 403 forbidden, 404 absent, 409 conflict, and server-side 5xx failures. Never map every exception to 200 with an error hidden in the body.\n\n## Representation and caching\nContent-Type describes the body sent; Accept expresses desired response formats. Cache-Control and validators such as ETag govern reuse and conditional requests. Define pagination and maximum page size for collections.\n\n## Practice\nWrite request/response examples for create, read, update, and delete. State which operations can be retried after an unknown outcome. Include an error schema with stable codes and safe messages. Document compatibility for adding a field and for changing an existing field's meaning.",
      "exercises": [
        {
          "id": "m18-l2-check",
          "type": "multipleChoice",
          "title": "HTTP methods, status codes, headers, media types, and idempotency: concept check",
          "instructionMarkdown": "What does an idempotent HTTP operation promise?",
          "choices": [
            "Repeating its intended effect is safe",
            "Every response byte is identical",
            "It cannot fail"
          ],
          "correctChoice": 0,
          "hints": [
            "Idempotency promises the resource's end state after repetition, not that every response body or status looks identical."
          ]
        }
      ]
    },
    {
      "id": "m18-l03-java-httpclient-synchronous-and-asynchronous-workflows",
      "title": "Java HttpClient synchronous and asynchronous workflows",
      "theoryMarkdown": "# Java HttpClient synchronous and asynchronous workflows\n\n## Reuse a configured client\n```java\nvar client = java.net.http.HttpClient.newBuilder()\n    .connectTimeout(java.time.Duration.ofSeconds(2)).build();\nvar request = java.net.http.HttpRequest.newBuilder(\n        java.net.URI.create(\"https://example.test/tasks\"))\n    .timeout(java.time.Duration.ofSeconds(5))\n    .header(\"Accept\", \"application/json\").GET().build();\nvar response = client.send(request,\n    java.net.http.HttpResponse.BodyHandlers.ofString());\nif (response.statusCode() != 200)\n    throw new java.io.IOException(\"unexpected status \" + response.statusCode());\n```\nexample.test is a placeholder; this external lab needs a controlled endpoint. send blocks and declares IOException/InterruptedException. sendAsync returns a CompletableFuture. An HTTP 404 is a received response, not automatically a transport exception.\n\n## Bound the body\nofString buffers the complete body and is suitable only when the response size is trusted or otherwise bounded. For untrusted large responses, stream with a byte limit and close the body. Encode URI components correctly rather than concatenating arbitrary input into a URL.\n\n## Practice\nUse a local stub endpoint producing 200, 404, slow, and oversized responses. Verify status handling, charset policy, cancellation, and closure. Restrict redirect and destination behavior when URLs are influenced by users to avoid requests to unintended internal services.",
      "exercises": [
        {
          "id": "m18-l3-check",
          "type": "multipleChoice",
          "title": "Java HttpClient synchronous and asynchronous workflows: concept check",
          "instructionMarkdown": "Is an HTTP 404 necessarily a transport exception in HttpClient?",
          "choices": [
            "Yes; send always throws for 4xx",
            "Yes; only 200 responses have bodies",
            "No; it is a received response whose status needs handling"
          ],
          "correctChoice": 2,
          "hints": [
            "Check what HttpClient.send actually throws versus what it returns, and where a non-2xx status code fits in that distinction."
          ]
        }
      ]
    },
    {
      "id": "m18-l04-json-types-schemas-parsing-limits-dtos-and-compatibility",
      "title": "JSON types, schemas, parsing limits, DTOs, and compatibility",
      "theoryMarkdown": "# JSON types, schemas, parsing limits, DTOs, and compatibility\n\n## JSON is a transport data model\nJSON supports objects, arrays, strings, numbers, booleans, and null. It has no native LocalDate, UUID, or Java integer-width type.\n```json\n{\"id\":\"42\",\"title\":\"Review\",\"dueDate\":\"2024-12-31\",\"tags\":[\"java\"]}\n```\nDefine whether IDs are strings, how timestamps are formatted, and whether a missing field differs from an explicit null. Use a maintained parser such as a declared Jackson or Gson dependency; Java 21's standard library does not provide a general JSON object-mapping API. Production Java code almost always binds JSON through one of these libraries' object mappers rather than hand-writing a parser, since correct escaping, streaming, and schema validation are easy to get wrong by hand.\n\n## Parse, validate, map\nTransport DTOs should expose only approved fields. Bound body size and nesting, define duplicate-key behavior, validate required values, then construct domain objects. Avoid polymorphic deserialization driven by arbitrary type names supplied by the input. Handwritten quote replacement is not a complete JSON encoder: control characters and escaping rules matter.\n\n## Practice\nDefine a task schema and fixtures for missing title, null title, unknown fields, duplicate IDs, Unicode, and control characters. Round-trip valid fixtures through a real library in an external project. Keep old-version fixtures to test compatibility. Do not claim that a String-formatting exercise validates a JSON boundary.",
      "exercises": [
        {
          "id": "m18-l4-check",
          "type": "multipleChoice",
          "title": "JSON types, schemas, parsing limits, DTOs, and compatibility: concept check",
          "instructionMarkdown": "Does Java 21's standard library provide a general JSON object mapper?",
          "choices": [
            "Yes; Serializable is JSON",
            "No; use an appropriate declared library",
            "Yes; String.replace is the object mapper"
          ],
          "correctChoice": 1,
          "hints": [
            "Recall what java.util ships for tokenizing text versus what a declared Jackson or Gson dependency provides for schema-aware object mapping."
          ]
        }
      ]
    },
    {
      "id": "m18-l05-retries-deadlines-backoff-jitter-circuit-breakers-and-bulkhe",
      "title": "Retries, deadlines, backoff, jitter, circuit breakers, and bulkheads",
      "theoryMarkdown": "# Retries, deadlines, backoff, jitter, circuit breakers, and bulkheads\n\n## Retries need an effect model\nA timeout means the result is unknown: the remote server may already have committed. Retrying a payment without an idempotency key can charge twice. Retry only selected transient failures and operations whose repetition is safe.\n\n```java\nlong cappedDelay = Math.min(30_000L, 1_000L << Math.min(attempt, 5));\n// Choose a random delay from 0 through cappedDelay for full jitter.\n```\nValidate nonnegative attempt values. Backoff reduces retry pressure; jitter prevents synchronized clients from retrying together. Apply a maximum attempt count and total deadline, not only a per-attempt timeout. Respect server retry guidance where appropriate.\n\n## Other controls\nA circuit breaker temporarily stops calls after failure thresholds. A bulkhead isolates capacity so one dependency cannot consume all workers. Neither replaces authentication, timeouts, or bounded queues.\n\n## Practice\nSimulate a request that commits and then loses its response. Design an idempotency record scoped to the caller and operation, with payload matching and expiry rules. Test permanent rejection, transient failure, exhausted deadline, and duplicate requests. Record retry counts without logging sensitive payloads.",
      "exercises": [
        {
          "id": "m18-l5-check",
          "type": "multipleChoice",
          "title": "Retries, deadlines, backoff, jitter, circuit breakers, and bulkheads: concept check",
          "instructionMarkdown": "What can a timeout after a payment request mean?",
          "choices": [
            "The server may have committed but the response was lost",
            "The server definitely did nothing",
            "The payment definitely succeeded"
          ],
          "correctChoice": 0,
          "hints": [
            "A timeout tells you the response was lost, not what the server actually did with the request before the connection dropped."
          ]
        }
      ]
    },
    {
      "id": "m18-assessment",
      "title": "Chapter 18 assessment and deliberate practice",
      "theoryMarkdown": "# Chapter 18 assessment\n\nApply this chapter's boundary and failure-classification reasoning to the decision scenario, then repair **Inspect status before treating a response as success** with the same status-code and retry-safety discipline the HTTP, HttpClient, JSON, and resilience lessons modeled. Write a prediction for what the broken status check will report before running the debugger, and preserve the intended success/failure classification when fixing it.\n\nThe function exercise is a bounded practice task, not proof of every chapter competency. Complete the implementation and external practice described in the five lessons as well. Review the changed source for its contract, then explain the relevant tradeoff without consulting the answer.",
      "exercises": [
        {
          "id": "m18-judgment",
          "type": "multipleChoice",
          "title": "Networking, HTTP, and JSON Boundaries: professional judgment",
          "instructionMarkdown": "A remote write times out. What must a retry policy consider?",
          "choices": [
            "The write may already have committed",
            "Timeout proves no effect",
            "Retry is always safe"
          ],
          "correctChoice": 0
        },
        {
          "id": "m18-coding-lab",
          "type": "function",
          "title": "Chapter 18 implementation lab: retryDelay",
          "instructionMarkdown": "Implement `retryDelay`. Return exponential delay min(1000 * 2^attempt, 30000); treat negative attempts as zero. Write the precondition and boundary table first, keep the method deterministic, and do not print.\n\nClamp negative attempts to zero and cap at 30000 before a shift could overflow. All int attempts are permitted.",
          "starterFiles": [
            {
              "path": "Solution.java",
              "content": "public final class Solution {\n    public static int retryDelay(int value1) {\n        return 0;\n    }\n}\n"
            }
          ],
          "entrypoint": {
            "kind": "function",
            "className": "Solution",
            "name": "retryDelay",
            "parameters": [
              {
                "name": "value1",
                "type": "int"
              }
            ],
            "returns": "int"
          },
          "tests": [
            {
              "id": "m18-code-1",
              "visibility": "public",
              "arguments": [
                0
              ],
              "expected": 1000
            },
            {
              "id": "m18-code-2",
              "visibility": "public",
              "arguments": [
                1
              ],
              "expected": 2000
            },
            {
              "id": "m18-code-3",
              "visibility": "hidden",
              "arguments": [
                5
              ],
              "expected": 30000
            },
            {
              "id": "m18-code-4",
              "visibility": "hidden",
              "arguments": [
                -1
              ],
              "expected": 1000
            },
            {
              "id": "m18-code-5",
              "visibility": "hidden",
              "arguments": [
                30
              ],
              "expected": 30000
            },
            {
              "id": "m18-code-6",
              "visibility": "hidden",
              "arguments": [
                2147483647
              ],
              "expected": 30000
            }
          ],
          "hints": [
            "Clamp the attempt count itself (for example to 5) before shifting, so the shift amount cannot overflow before the min() cap applies.",
            "Treat negative attempts as zero before computing 1000 << attempt, then cap the result at 30000."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        },
        {
          "id": "m18-debug-lab",
          "type": "debug",
          "title": "Inspect status before treating a response as success",
          "instructionMarkdown": "Repair the supplied defect so the program writes exactly `ERROR`. The status check currently treats any code under 500 as success, which misclassifies a client error like 404; preserve the branching structure while correcting which status range counts as success, and explain why your fix changes the result instead of hard-coding the print. Output tests verify the printed result; preserving the intended status classification requires reading the source, not just matching output.",
          "starterFiles": [
            {
              "path": "Main.java",
              "content": "public class Main {\n    \n    public static void main(String[] args) throws Exception {\n        int status=404; System.out.print(status<500 ? \"SUCCESS\" : \"ERROR\");\n    }\n}\n"
            }
          ],
          "tests": [
            {
              "id": "m18-debug-test",
              "visibility": "public",
              "input": "",
              "expected": "ERROR",
              "comparison": "exact"
            }
          ],
          "hints": [
            "This contract recognizes only 2xx as success, not every non-5xx response."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        }
      ]
    }
  ]
}
