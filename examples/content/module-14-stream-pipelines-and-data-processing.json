{
  "id": "module-14-stream-pipelines-and-data-processing",
  "title": "Chapter 14: Stream Pipelines and Data Processing",
  "description": "Design lazy, single-use data pipelines whose types and costs remain understandable. Learners trace source-to-terminal execution, distinguish map from flatMap, prove reduction associativity, build collision-aware and downstream collectors, close resource-backed streams, and reject parallelism when ordering, interference, blocking, or small workloads erase its value.",
  "lessons": [
    {
      "id": "m14-l01-stream-sources-single-use-lifecycle-laziness-and-terminal-op",
      "title": "Stream sources, single-use lifecycle, laziness, and terminal operations",
      "theoryMarkdown": "# Stream sources, single-use lifecycle, laziness, and terminal operations\n\n## A stream describes a traversal\n```java\nvar values = java.util.List.of(1, 2, 3, 4);\nlong count = values.stream()\n    .filter(n -> n % 2 == 0)\n    .count();\nSystem.out.println(count); // 2\n```\nThe collection owns elements; the stream describes their computation. filter is intermediate and lazy. count is terminal and drives traversal. A stream cannot be reused after a terminal operation; obtain another stream from the source.\n\nOperations may fuse so each element moves through several stages before the next is processed. Do not assume a stage runs once for every source element: short-circuit operations and optimizations can change traversal. Side effects used for essential behavior are therefore risky.\n\n## Practice\nUse anyMatch to find an even value and trace where it can stop. Contrast it with count, which needs the full relevant input. Attempt to reuse a consumed stream and explain the exception. Compare a loop with the pipeline and state which communicates the requirement more directly.",
      "exercises": [
        {
          "id": "m14-l1-check",
          "type": "multipleChoice",
          "title": "Stream sources, single-use lifecycle, laziness, and terminal operations: concept check",
          "instructionMarkdown": "What starts ordinary lazy stream traversal?",
          "choices": [
            "Creating the stream variable",
            "Declaring a filter alone",
            "A terminal operation"
          ],
          "correctChoice": 2,
          "hints": [
            "Intermediate operations such as filter are only recorded lazily; traversal starts when a terminal operation such as count is invoked."
          ]
        }
      ]
    },
    {
      "id": "m14-l02-filter-map-flatmap-distinct-sorted-and-peek",
      "title": "filter, map, flatMap, distinct, sorted, and peek",
      "theoryMarkdown": "# filter, map, flatMap, distinct, sorted, and peek\n\n## Transform shape deliberately\nfilter retains or drops an element. map emits one result per element. flatMap emits zero or more results and flattens their streams.\n```java\nvar lines = java.util.List.of(\"java sql\", \"java\");\nvar words = lines.stream()\n    .flatMap(line -> java.util.Arrays.stream(line.split(\" \")))\n    .filter(word -> !word.isEmpty())\n    .distinct()\n    .sorted()\n    .toList();\nSystem.out.println(words); // [java, sql]\n```\nUsing map instead would produce a stream of arrays or streams rather than a flat sequence of words. distinct uses equality and hashing; sorted uses ordering. Both may need state and memory proportional to input.\n\npeek is intended for observation and is not a reliable place for required persistence or business mutation. Stream.toList returns an unmodifiable list; do not assume it is an ArrayList.\n\n## Practice\nFlatten departments into employees, filter active employees, and map to names. Write down each stage's type. Test no departments, an empty department, and repeated names. Decide whether distinct names should collapse different people or whether identity must remain visible.",
      "exercises": [
        {
          "id": "m14-l2-check",
          "type": "multipleChoice",
          "title": "filter, map, flatMap, distinct, sorted, and peek: concept check",
          "instructionMarkdown": "Which operation flattens nested element streams?",
          "choices": [
            "peek",
            "flatMap",
            "map without flattening"
          ],
          "correctChoice": 1,
          "hints": [
            "flatMap maps each element to its own stream and flattens all of those streams into one, unlike map which would leave nested streams or arrays."
          ]
        }
      ]
    },
    {
      "id": "m14-l03-reduction-associativity-identity-and-primitive-streams",
      "title": "Reduction, associativity, identity, and primitive streams",
      "theoryMarkdown": "# Reduction, associativity, identity, and primitive streams\n\n## Reduction needs algebraic rules\n```java\nint total = java.util.stream.IntStream.of(2, 4, 6)\n    .reduce(0, Integer::sum);\nSystem.out.println(total); // 12\n```\nZero is the identity: adding it changes nothing. Addition is associative under the chosen arithmetic semantics, permitting regrouping. Subtraction is not associative, so reduce(0, (a,b)->a-b) does not define a safe parallel sum-like reduction.\n\nPrimitive streams avoid boxing and provide sum, average, min, max, and summaryStatistics. Empty min/max results are optional because no element exists. An average of an empty stream is not automatically zero.\n\n## Mutable accumulation\nUse collect with a fresh container supplier when accumulating into a mutable structure. Mutating a shared StringBuilder used as reduce's identity can combine shared state incorrectly in parallel execution.\n\n## Practice\nImplement sum, product, and maximum and choose each identity or absence representation. Trace different parenthesizations of subtraction. Compare sequential and parallel results without assuming equality proves safety for all inputs. State overflow and floating-point rounding policies; mathematical associativity does not eliminate representation effects.",
      "exercises": [
        {
          "id": "m14-l3-check",
          "type": "multipleChoice",
          "title": "Reduction, associativity, identity, and primitive streams: concept check",
          "instructionMarkdown": "Why is subtraction unsafe as an associative parallel reduction?",
          "choices": [
            "Regrouping changes its result",
            "Subtraction needs an explicit synchronized block to run safely in parallel",
            "The identity value must be nonzero for reduce to compile"
          ],
          "correctChoice": 0,
          "hints": [
            "Associativity requires that regrouping operands produce the same result; (a-b)-c differs from a-(b-c) in general, so parallel regrouping is unsafe."
          ]
        }
      ]
    },
    {
      "id": "m14-l04-collectors-grouping-partitioning-mapping-and-merge-policies",
      "title": "Collectors, grouping, partitioning, mapping, and merge policies",
      "theoryMarkdown": "# Collectors, grouping, partitioning, mapping, and merge policies\n\n## Collectors describe result construction\n```java\nrecord Sale(String region, int cents) {}\nvar sales = java.util.List.of(new Sale(\"EU\", 100), new Sale(\"EU\", 200));\nvar totals = sales.stream().collect(\n    java.util.stream.Collectors.groupingBy(\n        Sale::region, java.util.stream.Collectors.summingInt(Sale::cents)));\nSystem.out.println(totals.get(\"EU\")); // 300\n```\ngroupingBy chooses keys; its downstream collector reduces each group. partitioningBy always represents boolean partitions. mapping transforms elements before another collector receives them.\n\ntoMap requires a merge function when duplicate keys are possible. Choosing “keep first” versus “keep last” versus “sum” is a domain policy, not merely a fix for an exception. Supply a map factory when iteration order matters; defaults should not become accidental API promises.\n\n## Practice\nCreate counts by status, names by team, and totals by currency. Do not add amounts from different currencies into one meaningless sum. Test empty input and duplicate identifiers. Explain the result type at every collector nesting level and state whether callers may mutate the returned structures.",
      "exercises": [
        {
          "id": "m14-l4-check",
          "type": "multipleChoice",
          "title": "Collectors, grouping, partitioning, mapping, and merge policies: concept check",
          "instructionMarkdown": "What must a toMap collector specify when duplicate keys are allowed?",
          "choices": [
            "A random key order",
            "A custom hashCode override on the key type",
            "A merge policy"
          ],
          "correctChoice": 2,
          "hints": [
            "Collectors.toMap throws on a duplicate key unless a merge function is supplied to decide how the colliding values combine."
          ]
        }
      ]
    },
    {
      "id": "m14-l05-infinite-streams-resource-streams-parallelism-and-interferen",
      "title": "Infinite streams, resource streams, parallelism, and interference",
      "theoryMarkdown": "# Infinite streams, resource streams, parallelism, and interference\n\n## Boundedness and resources\n```java\nvar firstFive = java.util.stream.Stream.iterate(1, n -> n + 1)\n    .limit(5).toList();\ntry (var lines = java.nio.file.Files.lines(path)) {\n    long matches = lines.filter(line -> line.contains(\"ERROR\")).count();\n}\n```\nThe generated stream is infinite unless bounded or short-circuited. Sorting an unbounded source cannot finish. Files.lines owns an open resource and must be closed even when traversal fails.\n\n## Parallel judgment\nParallel streams divide work and combine results. Operations must avoid interference with the source and shared mutable state. CPU-heavy independent work may benefit; blocking I/O, small inputs, synchronization, and ordering can erase the gain. The default parallel execution environment is shared, so use explicit task management when isolation and cancellation matter.\n\nJava 21 does not include the later Stream Gatherers API as a stable feature. Keep course examples within the declared baseline.\n\n## Practice\nCompare filter().limit() and limit().filter() on a generated source. Explain why they can return different numbers of results. Benchmark a realistic pure CPU pipeline and record workload size and environment before claiming a speedup.",
      "exercises": [
        {
          "id": "m14-l5-check",
          "type": "multipleChoice",
          "title": "Infinite streams, resource streams, parallelism, and interference: concept check",
          "instructionMarkdown": "Why must Files.lines be closed?",
          "choices": [
            "Closing is needed to start filtering",
            "Its stream owns an external file resource",
            "Buffered readers close automatically when the JVM exits"
          ],
          "correctChoice": 1,
          "hints": [
            "Files.lines opens a file handle behind the returned stream, so it must be closed (ideally via try-with-resources) even if the pipeline never fully consumes it."
          ]
        }
      ]
    },
    {
      "id": "m14-assessment",
      "title": "Chapter 14 assessment and deliberate practice",
      "theoryMarkdown": "# Chapter 14 assessment\n\nBefore touching the debugger, use this chapter's rules on map versus flatMap to predict what **Flatten words before counting** should print, then run it and repair the defect so nested per-line streams are flattened before counting.\n\nThe evenSquares function lab exercises one reduction in isolation; it is not proof of every competency from stream laziness through parallel-safety judgment covered across the chapter's five lessons. Review the repaired source and explain why counting un-flattened streams gave the wrong total, without consulting the answer.",
      "exercises": [
        {
          "id": "m14-judgment",
          "type": "multipleChoice",
          "title": "Stream Pipelines and Data Processing: professional judgment",
          "instructionMarkdown": "A parallel reduction gives inconsistent results. What should be checked?",
          "choices": [
            "Associativity and shared mutable state",
            "Only the terminal method's spelling",
            "Only whether the stream was created with parallel() or parallelStream()"
          ],
          "correctChoice": 0
        },
        {
          "id": "m14-coding-lab",
          "type": "function",
          "title": "Chapter 14 implementation lab: evenSquares",
          "instructionMarkdown": "Implement `evenSquares`. Return the sum of the squares of all even integers in the input. Write the precondition and boundary table first, keep the method deterministic, and do not print.\n\nInput is non-null; all squared values and their sum in supplied tests fit int.",
          "starterFiles": [
            {
              "path": "Solution.java",
              "content": "public final class Solution {\n    public static int evenSquares(int[] value1) {\n        return 0;\n    }\n}\n"
            }
          ],
          "entrypoint": {
            "kind": "function",
            "className": "Solution",
            "name": "evenSquares",
            "parameters": [
              {
                "name": "value1",
                "type": "int[]"
              }
            ],
            "returns": "int"
          },
          "tests": [
            {
              "id": "m14-code-1",
              "visibility": "public",
              "arguments": [
                [
                  1,
                  2,
                  3,
                  4
                ]
              ],
              "expected": 20
            },
            {
              "id": "m14-code-2",
              "visibility": "hidden",
              "arguments": [
                []
              ],
              "expected": 0
            },
            {
              "id": "m14-code-3",
              "visibility": "hidden",
              "arguments": [
                [
                  -2,
                  0,
                  3
                ]
              ],
              "expected": 4
            },
            {
              "id": "m14-code-4",
              "visibility": "hidden",
              "arguments": [
                [
                  1,
                  3,
                  5
                ]
              ],
              "expected": 0
            }
          ],
          "hints": [
            "Filter for evenness, including negative even numbers and zero, before squaring and accumulating the sum.",
            "Check an empty array, an array with no even numbers, and an array containing a negative even number before submitting."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        },
        {
          "id": "m14-debug-lab",
          "type": "debug",
          "title": "Flatten words before counting",
          "instructionMarkdown": "Repair the pipeline so per-line word streams are flattened into one stream of words instead of being counted as nested stream objects, and the program writes exactly `3`. Keep the same split-and-count structure and explain why flatMap versus map changes what count() measures, rather than hard-coding the print statement. Output tests verify the printed result; confirming the words are genuinely flattened requires reading the source.",
          "starterFiles": [
            {
              "path": "Main.java",
              "content": "public class Main {\n    \n    public static void main(String[] args) throws Exception {\n        long n=java.util.List.of(\"java sql\",\"java\").stream().map(s->java.util.Arrays.stream(s.split(\" \"))).count(); System.out.print(n);\n    }\n}\n"
            }
          ],
          "tests": [
            {
              "id": "m14-debug-test",
              "visibility": "public",
              "input": "",
              "expected": "3",
              "comparison": "exact"
            }
          ],
          "hints": [
            "map counts nested streams; flatten them to count words."
          ],
          "limits": {
            "timeoutMs": 3000,
            "memoryMb": 256,
            "maxOutputKb": 64
          }
        }
      ]
    }
  ]
}
